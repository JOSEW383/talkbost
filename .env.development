# Database Configuration
DB_USER=talkbost
DB_PASSWORD=talkbost2025
DB_NAME=talkbost_db
DB_PORT=5432

# Backend Configuration
SECRET_KEY=your-secret-key-change-in-production-min-32-chars
BACKEND_PORT=8000

# API Configuration
PUBLIC_API_URL=http://localhost:8000

# Allowed Hosts
ALLOWED_HOSTS_FRONTEND=http://localhost:4321
ALLOWED_HOSTS_ADMIN=http://localhost:4322

# Ports Configuration
FRONTEND_PORT=4321
ADMIN_PORT=4322

# Build Configuration
BACKEND_COMMAND=uvicorn main:app --host 0.0.0.0 --port 8000 --reload
FRONTEND_COMMAND=npm run dev -- --host 0.0.0.0
ADMIN_COMMAND=npm run dev -- --host 0.0.0.0 --port 4322
