# Database Configuration
DB_USER=talkbost
DB_PASSWORD=CHANGE_THIS_IN_PRODUCTION
DB_NAME=talkbost_db
DB_PORT=5432

# Backend Configuration
SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS
BACKEND_PORT=8000

# API Configuration
PUBLIC_API_URL=https://api.talkbost.dedyn.io

# Allowed Hosts
ALLOWED_HOSTS_FRONTEND=https://talkbost.dedyn.io
ALLOWED_HOSTS_ADMIN=https://admin.talkbost.dedyn.io

# Ports Configuration
FRONTEND_PORT=4321
ADMIN_PORT=4322

# Build Configuration
BACKEND_COMMAND=uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
FRONTEND_COMMAND=npm run preview -- --host 0.0.0.0
ADMIN_COMMAND=npm run preview -- --host 0.0.0.0 --port 4322
